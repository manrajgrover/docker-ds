version: '3'
services:
  datascience:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    image: docker-ds:gpu
    env_file:
        - .env
    volumes:
        - "${project}:/root/project"
